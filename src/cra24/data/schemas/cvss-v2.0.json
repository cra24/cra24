{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://www.first.org/cvss/cvss-v2.0.json",
  "title": "Permissive local stand-in for the CVSS v2.0 schema",
  "x-cra24-note": "cra24 does not emit CVSS v2.0. This stand-in exists only so the CSAF 2.0 schema resolves offline. It accepts any object. Replace with the upstream first.org schema if you need real v2.0 validation.",
  "type": "object",
  "properties": {"version": {"type": "string", "enum": ["2.0"]}}
}
